Luke Darrow 11349190
WebDev HW 2

The username and password for the admin login is "username" and "password".

As discussed with Adam, only requirements that have been explicitly stated
and not suggested or required are implemented. The placeholder survey page
has been scraped entirely as such. The admin page has been implemented to
serve its functionality, which is to export the csv file once the admin logs
on. All fields are checked except the hours as the format for this field
is not specified in the document. Therefore, the user can input their available
hours in any alpha numeric format.

Overall, I've put alot of hours into this and have met the requirements described
by the document to the best of my ability.
